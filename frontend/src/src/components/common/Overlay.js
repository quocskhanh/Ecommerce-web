import React from 'react';

const Overlay = () => {
    return (
        <div className="overlay fixed inset-0 z-40 bg-black bg-opacity-10 rounded-3xl
        opacity-0 bg-opacity-10 invisible"></div>

    );
};

export default Overlay;